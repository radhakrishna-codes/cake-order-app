# Cake Order API (Python)

Backend placeholder for the cake order app. The frontend currently saves orders locally in the browser/Electron preview.

## Planned stack

- FastAPI
- SQLite or PostgreSQL for order storage
- Image upload handling for reference photos

## Next steps

1. Scaffold FastAPI app with order CRUD endpoints
2. Connect the React form to `POST /api/orders`
3. Add CORS for local dev and mobile web clients
